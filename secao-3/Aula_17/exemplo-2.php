<?php

    $valorTotal = 0;

    $valorTotal += 100; // Usando + antes do = somamos e atribuimos o valor da variavel atual com o da anterior

    $valorTotal += 25;

    //$valorTotal -= 10; // Subtraimos o o valor da variavel anterior com o da atual

    $valorTotal *= .9; // Multiplico e adciono uma casa decimal

    echo $valorTotal;

?>